# Hugo博客发布器部署教程

本教程将指导您完成 Hugo 博客发布器的完整部署过程。我们将后端部署到 Railway，前端部署到 Cloudflare Pages。

## 准备工作

在开始部署之前，您需要准备好以下账号和密钥：

1. **GitHub 账号** - 已有的
2. **Railway 账号** - 注册地址：https://railway.app
3. **Cloudflare 账号** - 注册地址：https://www.cloudflare.com
4. **DeepSeek API Key** - 获取地址：https://platform.deepseek.com
5. **GitHub Personal Access Token** - 用于访问您的博客仓库

---

## 第一步：准备 GitHub Personal Access Token

这个 Token 用于让发布器访问您的 GitHub 博客仓库并上传文件。

### 获取 Token 的步骤：

1. 打开 GitHub，点击右上角头像 → **Settings**

2. 左侧菜单向下滚动，点击 **Developer settings**

3. 点击 **Personal access tokens** → **Tokens (classic)**

4. 点击 **Generate new token (classic)**

5. 设置 Token 名称（建议：`hugo-publisher-token`）

6. 设置过期时间，建议选择 **No expiration**（永不过期）

7. 勾选以下权限：
   - ☑️ `repo` - 完整控制私有仓库（必须勾选）

8. 点击页面底部的 **Generate token**

9. **重要**：复制生成的 Token，它只显示一次，保存好

Token 格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

---

## 第二步：推送项目到 GitHub

如果项目还没有推送到 GitHub，请按以下步骤操作：

### 2.1 创建 GitHub 仓库

1. 打开 https://github.com/new

2. 仓库名称：`hugo-blog-publisher`（可以自定义）

3. 选择 **Private**（私有）或 **Public**（公开）

4. 不要勾选任何初始化选项

5. 点击 **Create repository**

### 2.2 本地初始化并推送

打开终端，进入项目目录：

```bash
cd /Users/ccy/Downloads/同步空间/VSCODE/榨利计算/hugo-blog-publisher

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit: Hugo blog publisher"

# 连接远程仓库（将 your-username 替换为您的 GitHub 用户名）
git remote add origin https://github.com/your-username/hugo-blog-publisher.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

---

## 第三步：部署后端到 Railway

Railway 是一个支持 Python 的云服务平台，非常适合部署我们的 Flask 后端。

### 3.1 创建 Railway 项目

1. 打开 https://railway.app 并登录

2. 点击 **New Project**

3. 选择 **Deploy from GitHub**

4. 选择您刚创建的 `hugo-blog-publisher` 仓库

5. Railway 会自动检测是 Python 项目

### 3.2 配置环境变量

1. 在 Railway 项目页面，点击 **Variables** 标签

2. 添加以下环境变量：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `DEEPSEEK_API_KEY` | `sk-xxxxx` | 您的 DeepSeek API Key |
| `GITHUB_TOKEN` | `ghp_xxxxx` | 第一步获取的 GitHub Token |
| `GITHUB_USERNAME` | `your-username` | 您的 GitHub 用户名 |
| `GITHUB_REPO` | `hugo-blog` | 您的 Hugo 博客仓库名 |
| `SECRET_KEY` | `任意随机字符串` | 用于加密，建议 32 位以上 |
| `FRONTEND_URL` | `https://xxx.pages.dev` | 前端地址（暂时填任意值，后面更新） |
| `PORT` | `5000` | Railway 会自动设置 |
| `DEBUG` | `false` | 生产环境设为 false |

3. 添加完后，Railway 会自动重新部署

### 3.3 配置启动命令

1. 在 Railway 页面，点击 **Settings** 标签

2. 找到 **Start Command**，设置为：
   ```
   gunicorn backend.app:app --bind 0.0.0.0:$PORT
   ```

3. Railway 已经预置了 `Procfile`，应该能自动识别

### 3.4 获取后端 API 地址

部署完成后，点击 **Deployments** 标签，复制类似这样的地址：
```
https://hugo-blog-publisher-production.up.railway.app
```

**保存好这个地址**，后面配置前端需要用到。

---

## 第四步：部署前端到 Cloudflare Pages

Cloudflare Pages 提供免费的静态网站托管，非常适合我们的前端应用。

### 4.1 连接 GitHub 仓库

1. 打开 https://www.cloudflare.com 并登录

2. 进入 **Workers & Pages** → **Pages** 标签

3. 点击 **Connect to Git**

4. 选择您的 GitHub 账号

5. 选择 `hugo-blog-publisher` 仓库

### 4.2 配置构建设置

在设置页面，填写以下内容：

| 配置项 | 值 |
|--------|-----|
| **Production branch** | `main` |
| **Build command** | （留空） |
| **Build output directory** | `frontend` |

### 4.3 设置环境变量（可选）

如果需要，可以在 **Environment variables** 添加：
- 无需添加任何变量

### 4.4 部署

1. 点击 **Save and Deploy**

2. 等待部署完成（通常 1-2 分钟）

3. 部署成功后，复制类似这样的地址：
   ```
   https://hugo-blog-publisher.pages.dev
   ```

**保存好这个地址**，需要更新后端配置。

---

## 第五步：更新后端的 CORS 配置

前端地址已确定，需要更新后端的 CORS 配置。

### 5.1 更新 Railway 环境变量

1. 打开 Railway 项目页面

2. 进入 **Variables** 标签

3. 找到 `FRONTEND_URL`，将值更新为您的 Cloudflare 前端地址：
   ```
   https://hugo-blog-publisher.pages.dev
   ```

4. Railway 会自动重新部署

---

## 第六步：更新前端配置文件

现在需要将前端配置中的后端 API 地址更新为 Railway 的实际地址。

### 6.1 编辑 frontend/config.js

```javascript
// 将这一行更新为您的 Railway 后端地址
const API_BASE_URL = 'https://您的Railway应用名.railway.app';
```

例如：
```javascript
const API_BASE_URL = 'https://hugo-blog-publisher-production.up.railway.app';
```

### 6.2 推送更新到 GitHub

```bash
git add frontend/config.js
git commit -m "Update API base URL for production"
git push
```

Cloudflare Pages 会自动重新部署。

---

## 第七步：测试部署结果

### 7.1 测试后端 API

在浏览器中打开：
```
https://您的Railway地址/api/health
```

如果返回：
```json
{"status":"ok","timestamp":"2024-12-25T10:30:00"}
```

说明后端正常工作。

### 7.2 测试前端页面

打开您的 Cloudflare Pages 地址：
```
https://hugo-blog-publisher.pages.dev
```

应该能看到博客发布器的界面。

### 7.3 完整流程测试

1. 在文章编辑区输入内容

2. 点击 **DeepSeek 优化排版** 按钮

3. 等待 AI 处理完成

4. 点击 **预览** 查看效果

5. 输入发布密码

6. 点击 **发布到 GitHub**

---

## 常见问题排查

### 问题 1：后端部署失败

**可能原因**：
- 环境变量配置错误
- Python 依赖安装失败

**解决方法**：
1. 在 Railway 的 **Deployments** 标签查看日志
2. 检查 `requirements.txt` 中的依赖
3. 确认所有环境变量都已正确设置

### 问题 2：DeepSeek 排版失败

**可能原因**：
- API Key 无效
- API 余额不足

**解决方法**：
1. 确认 `DEEPSEEK_API_KEY` 环境变量正确
2. 检查 DeepSeek 账户余额
3. 查看 Railway 日志中的具体错误信息

### 问题 3：GitHub 上传失败

**可能原因**：
- Token 权限不足
- 仓库名称错误
- 目标目录不存在

**解决方法**：
1. 确认 Token 有 `repo` 权限
2. 确认 `GITHUB_REPO` 是正确的仓库名
3. 确认目标目录（如 `content/posts`）在仓库中存在

### 问题 4：CORS 跨域错误

**可能原因**：
- 前端地址没有添加到后端的 `FRONTEND_URL`

**解决方法**：
1. 在 Railway 中更新 `FRONTEND_URL` 环境变量
2. 等待自动重新部署

---

## 后续维护

### 更新代码

1. 本地修改代码

2. 推送到 GitHub：
   ```bash
   git add .
   git commit -m "描述您的更改"
   git push
   ```

3. Railway 和 Cloudflare 会自动重新部署

### 查看日志

- **后端日志**：Railway 项目 → Deployments → 点击最新部署 → 查看日志
- **前端部署历史**：Cloudflare → Workers & Pages → Pages → 查看部署记录

---

## 部署架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      用户浏览器                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│              Cloudflare Pages (前端)                         │
│          https://hugo-blog-publisher.pages.dev              │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTPS 请求
                          ▼
┌─────────────────────────────────────────────────────────────┐
│               Railway (后端 API)                             │
│        https://hugo-blog-publisher.railway.app              │
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐     │
│  │ DeepSeek    │  │  GitHub     │  │  图片上传       │     │
│  │ Service     │  │  Service    │  │  /api/upload-   │     │
│  │ /api/format │  │  /api/publish│  │  image          │     │
│  └─────────────┘  └─────────────┘  └─────────────────┘     │
└─────────────────────────┬───────────────────────────────────┘
                          │ GitHub API
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                    GitHub 仓库                               │
│          hugo-blog (您的 Hugo 博客仓库)                      │
│                                                              │
│  ├── content/posts/    ← 文章发布到这里                      │
│  ├── content/notes/    ← 笔记发布到这里                      │
│  └── static/images/    ← 图片上传到这里                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 联系支持

如果在部署过程中遇到问题：

1. 查看本文档的 **常见问题排查** 部分
2. 查看 Railway 部署日志获取详细错误信息
3. 在 GitHub 仓库中创建 Issue 寻求帮助

祝您部署顺利！🎉
